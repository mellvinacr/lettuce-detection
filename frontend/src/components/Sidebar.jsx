"use client"

// Deprecated placeholder: Sidebar is now inlined in page components.
// Keep a noop export to avoid import errors until all pages are updated.

const Sidebar = () => null

export default Sidebar
